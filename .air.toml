root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/main.exe ./cmd/radar"
  bin = "./tmp/main.exe"
  args_bin = ["-dev"]
  delay = 500
  include_ext = ["go", "toml"]
  include_dir = ["cmd", "internal"]
  exclude_dir = ["tmp"]
  stop_on_error = false

[misc]
  clean_on_exit = true